deny_patterns:
  - name: Private Key
    regex: "-----BEGIN (RSA|EC|DSA|OPENSSH) PRIVATE KEY-----"
  - name: AWS Secret
    regex: "AWS_SECRET_ACCESS_KEY"
  - name: SSH RSA
    regex: "ssh-rsa\s+[A-Za-z0-9+/=]+"
